import { DaysToReadableFormatPipe } from './days-to-readable-format.pipe';

describe('DaysToReadableFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysToReadableFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
